#!/bin/bash
# This script will help us download and process the source spreadsheet data
# We'll need to use the Google Sheets MCP in multiple smaller reads

echo "This script needs to be run through Claude Code's MCP tools"
echo "It will read the source spreadsheet in batches"
echo "Then organize by country and add campaign type column"
